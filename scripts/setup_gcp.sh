# Check gcloud CLI is installed, exit if not
# Run: gcloud auth login
# Run: gcloud config set project {PROJECT_ID}
# Run: gcloud auth application-default login
# cd into terraform/
# Run: terraform init
# Run: terraform plan
# Prompt user to confirm before applying
# Run: terraform apply -auto-approve
# Print success message with VM IP and bucket name
